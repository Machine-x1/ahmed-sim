const endpoints = {
  createToken: 'oauth/token',
  refreshToken: 'oauth/token/refresh',
  products: 'products',
  contact: 'contact',
  // next api
  init: 'api/init',
  apiProduct: 'api/product/',
  apiContact: 'api/contact',
};

export { endpoints };
